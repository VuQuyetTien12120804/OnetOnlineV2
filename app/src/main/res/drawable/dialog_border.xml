<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền hộp thoại -->
    <solid android:color="#9EF1FB" />

    <!-- Độ bo góc -->
    <corners android:radius="30dp" />

    <!-- Viền -->
    <stroke
        android:width="5dp"
        android:color="#FF5722" />

</shape>
